import React, { useEffect, useState } from 'react'
import { useParams } from 'react-router-dom'

export default function ChatModal({currentUser}) {
  const {receiver_id} = useParams();

  use
  return (
    <div className="pop-up">
        Chat
    </div>
  )
}
